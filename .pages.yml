# Pages CMS config — experiment/pages-cms
#
# Editing happens on app.pagescms.org (no local admin). Content is stored as pure
# YAML (`.yaml`, `format: yaml`) — no frontmatter fences or body — read by
# Astro's glob loader and validated by the same src/content.config.ts Zod schema.
# `blockKey: type` discriminates the block union.

# Write Conventional Commit messages for edits made through the CMS (default is
# "Action {path} (via Pages CMS)"). {path} is the file the editor changed.
settings:
  commit:
    templates:
      create: 'feat(content): add {path}'
      update: 'chore(content): update {path}'
      delete: 'chore(content): remove {path}'
      rename: 'chore(content): rename {oldPath} to {newPath}'

# Images live in the repo under src/assets/media so Astro's <Image> component
# optimizes them at build time (hashed, immutable /_astro/* output). `input` is
# the repo storage folder; `output` is the path written into content — they
# intentionally differ. Content keeps referencing images as /media/foo.jpg;
# src/lib/images.ts maps that prefix to the src/assets/media asset at build.
# `image` fields and rich-text uploads browse/store here.
media:
  input: src/assets/media
  output: /media

# Custom buttons in the Pages CMS admin sidebar. "Publish" dispatches the
# cms-publish workflow against the current branch (ref: current), which merges
# that branch's open PR into main and then triggers the production deploy
# (cd-prod). The PR is opened automatically on save (cms-open-pr workflow) and
# cd-preview posts a preview, so this is the only manual step to go live.
actions:
  - name: publish
    label: Publish changes
    workflow: cms-publish.yml
    ref: current
    cancelable: false
    confirm: true

content:
  - name: pages
    label: Pages
    type: collection
    path: src/content/pages
    format: yaml
    filename: '{fields.slug}.yaml'
    view:
      fields: [title, slug]
      primary: title
    fields:
      - { name: title, label: Title, type: string, required: true }
      - { name: slug, label: URL slug, type: string }
      - { name: metaDescription, label: Meta description, type: text }
      - name: blocks
        label: Blocks
        type: block
        list: true
        blockKey: type
        blocks:
          # ---- hero ----
          - name: hero
            label: Hero
            fields:
              - {
                  name: variant,
                  label: Variant,
                  type: select,
                  options: { values: [fullscreen, standard] },
                }
              - { name: heading, label: Heading, type: string, required: true }
              - { name: subheading, label: Subheading, type: string }
              - { name: body, label: Body, type: text }
              - name: image
                label: Image
                type: object
                fields:
                  - { name: src, label: Image, type: image, required: true }
                  - { name: alt, label: Alt text, type: string }
                  - { name: width, label: Width, type: number }
                  - { name: height, label: Height, type: number }
              - name: ctas
                label: CTAs
                type: object
                list: true
                fields:
                  - { name: label, label: Label, type: string, required: true }
                  - { name: href, label: URL, type: string, required: true }
                  - {
                      name: variant,
                      label: Style,
                      type: select,
                      options:
                        {
                          values:
                            [
                              primary,
                              outline,
                              outline-inverse,
                              solid-inverse,
                              ghost,
                            ],
                        },
                    }
                  - { name: external, label: Opens in new tab, type: boolean }
          # ---- events (list injected at render time from the events collection) ----
          - name: events
            label: Events section
            fields:
              - { name: heading, label: Heading, type: string, required: true }
              - { name: intro, label: Intro, type: text }
              - {
                  name: limit,
                  label: Max events to show (soonest first; blank = all),
                  type: number,
                }
              - {
                  name: display,
                  label: Layout,
                  type: select,
                  options: { values: [grid, carousel] },
                }
              - name: viewAllCta
                label: '"View all" CTA'
                type: object
                fields:
                  - { name: label, label: Label, type: string }
                  - { name: href, label: URL, type: string }
                  - {
                      name: variant,
                      label: Style,
                      type: select,
                      options:
                        {
                          values:
                            [
                              primary,
                              outline,
                              outline-inverse,
                              solid-inverse,
                              ghost,
                            ],
                        },
                    }
                  - { name: external, label: Opens in new tab, type: boolean }
          # ---- sectionHeading ----
          - name: sectionHeading
            label: Section heading
            fields:
              - { name: heading, label: Heading, type: string, required: true }
              - { name: subheading, label: Subheading, type: text }
              - {
                  name: align,
                  label: Align,
                  type: select,
                  options: { values: [left, center] },
                }
              - {
                  name: background,
                  label: Background,
                  type: select,
                  options: { values: [base, raised, inverse, accent] },
                }
              - {
                  name: headingColor,
                  label: Heading color,
                  type: select,
                  options: { values: [accent, dark, inverse, muted] },
                }
              - {
                  name: variant,
                  label: Variant,
                  type: select,
                  options: { values: [heading, lead] },
                }
              - {
                  name: width,
                  label: Width,
                  type: select,
                  options: { values: [narrow, medium, wide] },
                }
          # ---- activityGrid ----
          - name: activityGrid
            label: Activity grid
            fields:
              - { name: heading, label: Heading, type: string }
              - { name: subheading, label: Subheading, type: string }
              - name: image
                label: Feature image
                type: object
                fields:
                  - { name: src, label: Image, type: image, required: true }
                  - { name: alt, label: Alt text, type: string }
                  - { name: width, label: Width, type: number }
                  - { name: height, label: Height, type: number }
              - name: items
                label: Items
                type: object
                list: true
                fields:
                  - { name: label, label: Label, type: string, required: true }
                  - { name: href, label: URL, type: string }
                  - name: icon
                    label: Icon
                    type: object
                    fields:
                      - { name: src, label: Image, type: image, required: true }
                      - { name: alt, label: Alt text, type: string }
                      - { name: width, label: Width, type: number }
                      - { name: height, label: Height, type: number }
          # ---- testimonial ----
          - name: testimonial
            label: Testimonial
            fields:
              - { name: quote, label: Quote, type: text, required: true }
              - { name: attribution, label: Attribution, type: string }
              - name: image
                label: Image
                type: object
                fields:
                  - { name: src, label: Image, type: image, required: true }
                  - { name: alt, label: Alt text, type: string }
          # ---- gallery ----
          - name: gallery
            label: Gallery
            fields:
              - {
                  name: layout,
                  label: Layout,
                  type: select,
                  options: { values: [grid, mosaic] },
                }
              - { name: columns, label: Columns, type: number }
              - {
                  name: paddingTop,
                  label: Padding top,
                  type: select,
                  options: { values: [default, small, none] },
                }
              - {
                  name: paddingBottom,
                  label: Padding bottom,
                  type: select,
                  options: { values: [default, small, none] },
                }
              - name: images
                label: Images
                type: object
                list: true
                fields:
                  - { name: src, label: Image, type: image, required: true }
                  - { name: alt, label: Alt text, type: string }
                  - { name: width, label: Width, type: number }
                  - { name: height, label: Height, type: number }
          # ---- followUs ----
          - name: followUs
            label: Follow us
            fields:
              - { name: heading, label: Heading, type: string }
              - {
                  name: background,
                  label: Background,
                  type: select,
                  options: { values: [base, raised] },
                }
              - {
                  name: paddingTop,
                  label: Padding top,
                  type: select,
                  options: { values: [default, small, large, xl, none] },
                }
              - {
                  name: paddingBottom,
                  label: Padding bottom,
                  type: select,
                  options: { values: [default, small, large, xl, none] },
                }
          # ---- textWithMedia ----
          - name: textWithMedia
            label: Text with media
            fields:
              - { name: eyebrow, label: Eyebrow, type: string }
              - { name: heading, label: Heading, type: string, required: true }
              - { name: body, label: Body, type: rich-text, required: true }
              - name: image
                label: Image
                type: object
                fields:
                  - { name: src, label: Image, type: image, required: true }
                  - { name: alt, label: Alt text, type: string }
                  - { name: width, label: Width, type: number }
                  - { name: height, label: Height, type: number }
              - {
                  name: mediaPosition,
                  label: Media position,
                  type: select,
                  options: { values: [left, right] },
                }
              - name: ctas
                label: CTAs
                type: object
                list: true
                fields:
                  - { name: label, label: Label, type: string, required: true }
                  - { name: href, label: URL, type: string, required: true }
                  - {
                      name: variant,
                      label: Style,
                      type: select,
                      options:
                        {
                          values:
                            [
                              primary,
                              outline,
                              outline-inverse,
                              solid-inverse,
                              ghost,
                            ],
                        },
                    }
                  - { name: external, label: Opens in new tab, type: boolean }
              - {
                  name: anchor,
                  label: Anchor ID,
                  type: string,
                  description: 'Optional. Lets links jump straight to this section, e.g. #make-a-donation',
                }
          # ---- timeline ----
          - name: timeline
            label: Timeline
            fields:
              - { name: eyebrow, label: Eyebrow, type: string }
              - { name: heading, label: Heading, type: string }
              - name: entries
                label: Entries
                type: object
                list: true
                fields:
                  - { name: date, label: Date, type: string, required: true }
                  - { name: title, label: Title, type: string, required: true }
                  - { name: body, label: Body, type: text, required: true }
          # ---- cta ----
          - name: cta
            label: Call to action
            fields:
              - { name: heading, label: Heading, type: string, required: true }
              - { name: body, label: Body, type: text }
              - {
                  name: background,
                  label: Background,
                  type: select,
                  options: { values: [light, dark, accent, bar] },
                }
              - name: ctas
                label: CTAs
                type: object
                list: true
                fields:
                  - { name: label, label: Label, type: string, required: true }
                  - { name: href, label: URL, type: string, required: true }
                  - {
                      name: variant,
                      label: Style,
                      type: select,
                      options:
                        {
                          values:
                            [
                              primary,
                              outline,
                              outline-inverse,
                              solid-inverse,
                              ghost,
                            ],
                        },
                    }
                  - { name: external, label: Opens in new tab, type: boolean }
          # ---- twoColumnText ----
          - name: twoColumnText
            label: Two-column text
            fields:
              - { name: leftHeading, label: Left heading, type: string }
              - {
                  name: leftBody,
                  label: Left body,
                  type: rich-text,
                  required: true,
                }
              - name: leftCtas
                label: Left CTAs
                type: object
                list: true
                fields:
                  - { name: label, label: Label, type: string, required: true }
                  - { name: href, label: URL, type: string, required: true }
                  - {
                      name: variant,
                      label: Style,
                      type: select,
                      options:
                        {
                          values:
                            [
                              primary,
                              outline,
                              outline-inverse,
                              solid-inverse,
                              ghost,
                            ],
                        },
                    }
                  - { name: external, label: Opens in new tab, type: boolean }
              - { name: rightHeading, label: Right heading, type: string }
              - {
                  name: rightBody,
                  label: Right body,
                  type: rich-text,
                  required: true,
                }
              - name: rightCtas
                label: Right CTAs
                type: object
                list: true
                fields:
                  - { name: label, label: Label, type: string, required: true }
                  - { name: href, label: URL, type: string, required: true }
                  - {
                      name: variant,
                      label: Style,
                      type: select,
                      options:
                        {
                          values:
                            [
                              primary,
                              outline,
                              outline-inverse,
                              solid-inverse,
                              ghost,
                            ],
                        },
                    }
                  - { name: external, label: Opens in new tab, type: boolean }
              - {
                  name: background,
                  label: Background,
                  type: select,
                  options: { values: [base, raised] },
                }
              - {
                  name: alignAcross,
                  label: Align across columns,
                  type: boolean,
                }
              - {
                  name: paddingTop,
                  label: Padding top,
                  type: select,
                  options: { values: [default, small, none] },
                }
              - {
                  name: paddingBottom,
                  label: Padding bottom,
                  type: select,
                  options: { values: [default, small, none] },
                }
              - name: ctas
                label: CTAs
                type: object
                list: true
                fields:
                  - { name: label, label: Label, type: string, required: true }
                  - { name: href, label: URL, type: string, required: true }
                  - {
                      name: variant,
                      label: Style,
                      type: select,
                      options:
                        {
                          values:
                            [
                              primary,
                              outline,
                              outline-inverse,
                              solid-inverse,
                              ghost,
                            ],
                        },
                    }
                  - { name: external, label: Opens in new tab, type: boolean }
          # ---- volunteerForm ----
          - name: volunteerForm
            label: Volunteer form
            fields:
              - { name: heading, label: Heading, type: string, required: true }
              - { name: body, label: Body, type: rich-text }
              - name: image
                label: Image
                type: object
                fields:
                  - { name: src, label: Image, type: image, required: true }
                  - { name: alt, label: Alt text, type: string }
              - {
                  name: mediaPosition,
                  label: Media position,
                  type: select,
                  options: { values: [left, right] },
                }
              - { name: action, label: Form action URL, type: string }
              - {
                  name: method,
                  label: Method,
                  type: select,
                  options: { values: [POST, GET] },
                }
              - { name: submitLabel, label: Submit label, type: string }
              - {
                  name: background,
                  label: Background,
                  type: select,
                  options: { values: [base, raised] },
                }
          # ---- richText ----
          - name: richText
            label: Rich text
            fields:
              - { name: body, label: Body, type: rich-text, required: true }
              - {
                  name: align,
                  label: Align,
                  type: select,
                  options: { values: [left, center] },
                }
              - {
                  name: width,
                  label: Width,
                  type: select,
                  options: { values: [narrow, medium, wide, full] },
                }
              - {
                  name: background,
                  label: Background,
                  type: select,
                  options: { values: [base, raised, inverse, accent] },
                }
              - {
                  name: paddingTop,
                  label: Padding top,
                  type: select,
                  options: { values: [default, small, large, xl, none] },
                }
              - {
                  name: paddingBottom,
                  label: Padding bottom,
                  type: select,
                  options: { values: [default, small, large, xl, none] },
                }
          # ---- projects ----
          - name: projects
            label: Projects
            fields:
              - { name: heading, label: Heading, type: string, required: true }
              - { name: intro, label: Intro, type: text }
              - name: projects
                label: Projects
                type: object
                list: true
                fields:
                  - { name: title, label: Title, type: string, required: true }
                  - { name: body, label: Body, type: text, required: true }
                  - name: image
                    label: Image
                    type: object
                    fields:
                      - { name: src, label: Image, type: image, required: true }
                      - { name: alt, label: Alt text, type: string }
                  - { name: caption, label: Caption, type: string }
          # ---- board ----
          - name: board
            label: Board directory
            fields:
              - { name: heading, label: Heading, type: string, required: true }
              - { name: intro, label: Intro, type: text }
              - name: members
                label: Members
                type: object
                list: true
                fields:
                  - { name: name, label: Name, type: string, required: true }
                  - { name: role, label: Role, type: string, required: true }
                  - { name: bio, label: Bio, type: text }
          # ---- partners ----
          - name: partners
            label: Partners
            fields:
              - { name: heading, label: Heading, type: string, required: true }
              - { name: intro, label: Intro, type: text }
              - name: partners
                label: Partners
                type: object
                list: true
                fields:
                  - { name: name, label: Name, type: string, required: true }
                  - { name: url, label: URL, type: string, required: true }
                  - name: logo
                    label: Logo
                    type: object
                    fields:
                      - { name: src, label: Image, type: image, required: true }
                      - { name: alt, label: Alt text, type: string }
                      - { name: width, label: Width, type: number }
                      - { name: height, label: Height, type: number }
          # ---- videoEmbed ----
          - name: videoEmbed
            label: Video embed
            fields:
              - {
                  name: youtubeId,
                  label: YouTube video ID,
                  type: string,
                  required: true,
                }
              - { name: title, label: Title, type: string, required: true }
              - { name: caption, label: Caption, type: string }
              - name: poster
                label: Poster image
                type: object
                fields:
                  - { name: src, label: Image, type: image, required: true }
                  - { name: alt, label: Alt text, type: string }
              - {
                  name: background,
                  label: Background,
                  type: select,
                  options: { values: [base, raised] },
                }
          # ---- newsletter ----
          - name: newsletter
            label: Newsletter signup
            fields:
              - { name: heading, label: Heading, type: string, required: true }
              - { name: subheading, label: Subheading, type: text }
              - {
                  name: background,
                  label: Background,
                  type: select,
                  options: { values: [base, raised, inverse, accent] },
                }
          # ---- mapEmbed ----
          - name: mapEmbed
            label: Map embed
            fields:
              - {
                  name: embedUrl,
                  label: Embed URL,
                  type: string,
                  required: true,
                }
              - { name: title, label: Title, type: string, required: true }
              - { name: maxHeight, label: Max height (px), type: number }
              - {
                  name: background,
                  label: Background,
                  type: select,
                  options: { values: [base, raised] },
                }
          # ---- contactForm ----
          - name: contactForm
            label: Contact form
            fields:
              - { name: heading, label: Heading, type: string }
              - { name: body, label: Intro, type: rich-text }
              - name: interests
                label: Interest options
                type: object
                list: true
                fields:
                  - { name: label, label: Label, type: string, required: true }
                  - { name: value, label: Value, type: string, required: true }
              - { name: action, label: Form action URL, type: string }
              - {
                  name: method,
                  label: Method,
                  type: select,
                  options: { values: [POST, GET] },
                }
              - { name: submitLabel, label: Submit label, type: string }
              - {
                  name: background,
                  label: Background,
                  type: select,
                  options: { values: [base, raised] },
                }
          # ---- donorbox ----
          - name: donorbox
            label: Donorbox embed
            fields:
              - {
                  name: campaignUrl,
                  label: Donorbox campaign URL,
                  type: string,
                  required: true,
                }
              - {
                  name: paypalExpress,
                  label: Enable PayPal Express,
                  type: boolean,
                }
              - { name: height, label: Height (px), type: number }
              - {
                  name: background,
                  label: Background,
                  type: select,
                  options: { values: [base, raised] },
                }
              - {
                  name: anchor,
                  label: Anchor ID,
                  type: string,
                  description: 'Optional. Lets links jump straight to this section, e.g. #make-a-donation',
                }
          # ---- donationPanel ----
          - name: donationPanel
            label: Donation panel
            fields:
              - { name: heading, label: Heading, type: string, required: true }
              - { name: body, label: Body, type: rich-text, required: true }
              - {
                  name: campaignUrl,
                  label: Donorbox campaign URL,
                  type: string,
                  required: true,
                }
              - {
                  name: anchor,
                  label: Anchor ID,
                  type: string,
                  description: 'Optional. Lets links jump straight to this section, e.g. #make-a-donation',
                }
          # ---- ecwidStore ----
          - name: ecwidStore
            label: Ecwid store
            fields:
              - {
                  name: storeId,
                  label: Ecwid store ID,
                  type: string,
                  required: true,
                }

  - name: events
    label: Events
    type: collection
    path: src/content/events
    format: yaml
    filename: '{fields.slug}.yaml'
    view:
      fields: [title, date]
      primary: title
    fields:
      - { name: title, label: Title, type: string, required: true }
      - {
          name: slug,
          label: URL slug,
          type: string,
          required: true,
          description: 'Used in the event page URL, e.g. goats-on-the-slope',
        }
      - {
          name: date,
          label: Date,
          type: date,
          required: true,
          options: { format: 'yyyy-MM-dd' },
          description: 'Pick the event date',
        }
      - {
          name: endDate,
          label: End date,
          type: date,
          options: { format: 'yyyy-MM-dd' },
          description: 'Only for multi-day events',
        }
      - { name: time, label: Time, type: string }
      - { name: location, label: Location, type: string }
      - {
          name: description,
          label: Short description,
          type: text,
          required: true,
          description: 'Shown on event cards and listings',
        }
      - {
          name: details,
          label: Full details,
          type: rich-text,
          description: 'Long-form description shown on the event detail page',
        }
      - name: image
        label: Image
        type: object
        fields:
          - { name: src, label: Image, type: image, required: true }
          - { name: alt, label: Alt text, type: string }
      - name: schedule
        label: Schedule of activities
        type: object
        list: true
        description: 'Per-day/per-activity agenda, grouped by date on the event page'
        fields:
          - {
              name: date,
              label: Date,
              type: date,
              required: true,
              options: { format: 'yyyy-MM-dd' },
              description: 'Pick the activity date',
            }
          - { name: time, label: Time, type: string }
          - { name: title, label: Title, type: string, required: true }
          - { name: description, label: Description, type: text }
          - name: image
            label: Photo
            type: object
            description: 'Optional photo shown alongside this activity'
            fields:
              - { name: src, label: Image, type: image, required: true }
              - { name: alt, label: Alt text, type: string }
          - name: cta
            label: Call to action
            type: object
            description: 'Optional button, e.g. RSVP or learn more'
            fields:
              - { name: label, label: Label, type: string }
              - { name: href, label: URL, type: string }
              - {
                  name: variant,
                  label: Style,
                  type: select,
                  options:
                    {
                      values:
                        [
                          primary,
                          outline,
                          outline-inverse,
                          solid-inverse,
                          ghost,
                        ],
                    },
                }
              - { name: external, label: Opens in new tab, type: boolean }
      - name: sponsorTiers
        label: Sponsor tiers
        type: object
        list: true
        description: 'Group sponsors by level (e.g. Advocate, Supporting, In-kind)'
        fields:
          - { name: tier, label: Tier name, type: string, required: true }
          - name: partners
            label: Sponsors
            type: object
            list: true
            fields:
              - { name: name, label: Name, type: string, required: true }
              - { name: url, label: URL, type: string, required: true }
              - name: logo
                label: Logo
                type: object
                fields:
                  - { name: src, label: Image, type: image, required: true }
                  - { name: alt, label: Alt text, type: string }
                  - { name: width, label: Width, type: number }
                  - { name: height, label: Height, type: number }
      - name: recurrence
        label: Recurrence
        type: object
        description: 'For recurring events — the site recomputes the next occurrence at build'
        fields:
          - {
              name: label,
              label: Cadence label,
              type: string,
              description: 'e.g. Second Sunday of every month',
            }
          - { name: weekday, label: Weekday (0 = Sun … 6 = Sat), type: number }
          - { name: week, label: Week of month (1–5), type: number }
